[tool.poetry]
name = "iddn_paper"
version = "0.1.0"
description = "Modules for the experiments in iDDN paper"
authors = ["Yizhi Wang"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = ">=3.9"

[tool.poetry.dev-dependencies]

[tool.poetry.group.dev.dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
